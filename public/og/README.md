# OpenGraph Assets

Recommended final assets:

- `default.png` — 1200x630 default site preview
- `tools.png` — 1200x630 developer tools preview
- `credits.png` — 1200x630 startup credits preview
- `directory.png` — 1200x630 platform directory preview

Current placeholder:

- `default.svg` — lightweight SVG fallback used by metadata until final PNG artwork is designed.

When final PNGs are available, update `app/layout.tsx` and page-specific metadata to reference the PNG versions.
